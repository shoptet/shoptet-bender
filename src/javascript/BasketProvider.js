export default class BasketProvider {
	constructor() {
		document.addEventListener('shoptet.helpers.updateQuantity', (e) => {});
		document.addEventListener('ShoptetCartAddCartItem', (e) => {});
	}
}
